using UnityEngine;
using System.Collections;

public class DrawPadlock : MonoBehaviour {


    /*public Texture padlock;
    public Texture padlockUnlocked;*/

	// Use this for initialization
	void Start () {

	}
	
	// Update is called once per frame
	void Update () {
	
	}

    void OnGUI()
    {
       /* if (PlayerScore.Score >= 5)

            GUI.DrawTexture(new Rect(1, 5, 300, 90), padlockUnlocked);

        else

            GUI.DrawTexture(new Rect(1, 5, 300, 90), padlock);*/

    }
}
